{
  "id": "duploctl",
  "version": "1.0.5",
  "name": "duploctl",
  "description": "Installs duploctl CLI via pip (if available) or prebuilt binary.",
  "options": {
    "version": {
      "type": "string",
      "default": "latest",
      "description": "Version to install (e.g. v0.3.8). If 'latest' and pip is unavailable, falls back to v0.3.8 for binary install."
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/python"
  ]
}
